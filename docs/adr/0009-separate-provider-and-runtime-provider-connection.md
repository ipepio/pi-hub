---
status: accepted
---

# Separate Provider from Runtime Provider Connection

A Provider is a definition and catalogue source. A Runtime Provider Connection is its effective binding inside one User Runtime. The connection may be built-in, `models.json`, managed, OAuth-backed, or supplied by an Extension. Its redacted observed state does not become a dashboard-global Model automatically.
