---
status: accepted
---

# Split desired and effective Provider authority

The dashboard owns the desired managed projection. pihub owns the effective state inside the Runtime and reports a redacted observation. A service-token replace operation is idempotent and capability is selected by an explicit Runtime Release matrix, never by probing with a destructive mutation.
